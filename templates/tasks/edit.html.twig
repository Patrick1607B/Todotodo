{% extends 'base.html.twig' %}

{% block title %}Edit Tasks
{% endblock %}

{% block body %}
	<div id="mainTable">
        <div class="conteneurCreation">
			<h2>Modifier les tâches</h2>

			{{ include('tasks/_form.html.twig', {'button_label': 'Update'}) }}
			{{ include('tasks/_delete_form.html.twig') }}
			<a href="{{ path('app_tasks_index', {'id': id}) }}">Retour a la liste</a>

		</div>
	</div>	
{% endblock %}
