<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		{% block stylesheets %}
			<link rel="stylesheet" href="{{ asset('style.css') }}">

		{% endblock %}


		{% block awesome %}<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css" integrity="sha384-B4dIYHKNBt8Bc12p+WXckhzcICo0wtJAoU8YZTY5qE0Id1GSseTk6S+L3BlXeVIU" crossorigin="anonymous">
		{% endblock %}

	</head>
	<body>
		<header>
			{# <img src="../asset/logo/logo.àng.png" alt="Logo Todo Todo"> #}
			<h1 class="animate-charcter">Todo Tout Doux</h1>
			
		</header>
		
			<!--conteneur des nuages-->
			<div id="contentCloud">
				<i id="cloud1" class="fa fa-cloud" aria-hidden="true"></i>
				<i id="cloud2" class="fa fa-cloud gris" aria-hidden="true"></i>
				<i id="cloud3" class="fa fa-cloud" aria-hidden="true"></i>
				<i id="cloud4" class="fa fa-cloud gris" aria-hidden="true"></i>
				<i id="cloud5" class="fa fa-cloud gris" aria-hidden="true"></i>
				<i id="cloud6" class="fa fa-cloud gris" aria-hidden="true"></i>
			</div>
			 {% block body %}{% endblock %}
				
		
		<footer>
			<p>Si tu veux chasser les nuages gris : clique dessus !</p>
			{% if app.user %}
				<div class="mb-3">
					Tu es connecté en tant que
					{{ app.user.userIdentifier }}, si tu veux sortir c'est ici
					<a href="{{ path('app_logout') }}"><img src="{{ asset('asset/img/porte-de-sortie.png') }}" 									alt="déconnexion"></a>
				</div>
			{% endif %}
			{% block Lien %}
					{% if is_granted('ROLE_ADMIN') %}
						<p>
							<a href="{{ path('app_register') }}">Enregistrement</a>
							<a href="{{ path('app_login') }}">Connexion</a>
							{# <a href="{{ path('app_tasks_index') }}">Les taches</a> #}
							<a href="{{ path('app_todo_lists_index') }}">Todolist</a>
							<a href="{{ path('app_user_index') }}">Utilisateur</a>
						</p>
					{% endif %}
			{% endblock %}
		</footer>
		<script src="{{ asset('script.js') }}"></script>
	</body>
</html>
